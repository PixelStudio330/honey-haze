generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  
}

model Food {
  id          Int      @id @default(autoincrement())
  name        String
  price       Int
  type        String
  description String
  image       String
}